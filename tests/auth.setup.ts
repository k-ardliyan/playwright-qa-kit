/**
 * Auth Setup Entrypoint
 *
 * Playwright setup project entrypoint. Discovers login-ready roles and materializes
 * storage state files into `.auth/{APP_ENV}/{role}.json`.
 */
import '../src/support/auth.setup';
