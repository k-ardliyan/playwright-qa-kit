import { test as base } from '@playwright/test';

/**
 * Project-specific Fixtures (template seam)
 *
 * Fork this file on day one and register Page Object Models (POM) for your app.
 */
export const projectTest = base.extend({});
